import React from 'react';
import styles from '../styles/Header.css';

function Header() {
    return (
      <header style={styles} className="header">
        <h1 style={styles}>Damien Armstrong</h1>
      </header>
    );
  }
  
  export default Header;